# Staging profile. Explicit origins required — fail-fast validation in
# DeploymentConfigValidator refuses to start with an empty or wildcard list.
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:}

# Staging behaves like production for security-relevant behaviour.
logging.level.com.gonosia.game=${LOG_LEVEL:INFO}